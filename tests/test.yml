---
- hosts: localhost
  remote_user: root
  roles:
    - { role: ansible-role-nginx,
          nginx_create_nginx_conf: true,
          nginx_http_params: { 
          },
          nginx_sites: []
      }
